import { useEffect, useState } from "react";
import { motion } from "framer-motion";

export default function RedactorIAVisual() {
  const [mouse, setMouse] = useState({ x: 0, y: 0 });
  const [active, setActive] = useState(false);

  useEffect(() => {
    const handleMove = (e) => {
      setMouse({ x: e.clientX, y: e.clientY });
      if (!active) setActive(true);
    };

    window.addEventListener("pointermove", handleMove);
    return () => window.removeEventListener("pointermove", handleMove);
  }, [active]);

  const presets = [
    { name: "Pulido", active: true },
    { name: "Ejecutivo" },
    { name: "Natural" },
    { name: "Creativo" },
    { name: "Cercano" },
    { name: "Premium" },
  ];

  return (
    <div className="relative min-h-screen overflow-hidden bg-[#0a0f1f] text-white">
      <div className="pointer-events-none absolute inset-0">
        <div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(56,189,248,0.18),transparent_24%),radial-gradient(circle_at_80%_10%,rgba(168,85,247,0.14),transparent_22%),radial-gradient(circle_at_50%_100%,rgba(244,114,182,0.12),transparent_30%)]" />
        <div className="absolute inset-0 opacity-[0.08] [background-image:linear-gradient(rgba(255,255,255,0.7)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.7)_1px,transparent_1px)] [background-size:72px_72px]" />

        <motion.div
          className="absolute h-80 w-80 rounded-full bg-cyan-400/20 blur-3xl"
          animate={{
            x: mouse.x - 160,
            y: mouse.y - 160,
            opacity: active ? 1 : 0,
          }}
          transition={{ type: "spring", stiffness: 70, damping: 24, mass: 0.9 }}
        />

        <motion.div
          className="absolute h-28 w-28 rounded-full border border-white/20 bg-white/8 backdrop-blur-md"
          animate={{
            x: mouse.x - 56,
            y: mouse.y - 56,
            opacity: active ? 1 : 0,
            scale: active ? 1 : 0.8,
          }}
          transition={{ type: "spring", stiffness: 180, damping: 20, mass: 0.45 }}
        />
      </div>

      <div className="relative z-10 mx-auto grid min-h-screen max-w-7xl grid-cols-1 lg:grid-cols-[300px_minmax(0,1fr)]">
        <aside className="border-b border-white/10 bg-black/20 p-5 backdrop-blur-xl lg:border-b-0 lg:border-r">
          <div className="flex h-full flex-col">
            <div>
              <div className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-white/70">
                <span className="h-2 w-2 rounded-full bg-emerald-400" />
                Solo visual
              </div>
              <h1 className="mt-5 text-3xl font-semibold tracking-tight">Redactor IA</h1>
              <p className="mt-3 text-sm leading-6 text-white/65">
                Nuevo layout con estructura tipo dashboard editorial y estilo oscuro neón.
              </p>
            </div>

            <div className="mt-8 space-y-3">
              <div className="rounded-3xl border border-white/10 bg-white/5 p-4">
                <p className="text-xs uppercase tracking-[0.22em] text-white/45">Sesión</p>
                <div className="mt-3 flex items-end justify-between">
                  <div>
                    <p className="text-3xl font-semibold">3</p>
                    <p className="text-sm text-white/55">intentos libres</p>
                  </div>
                  <div className="rounded-2xl bg-emerald-400/15 px-3 py-2 text-xs text-emerald-300">
                    5 totales
                  </div>
                </div>
              </div>

              <div className="rounded-3xl border border-white/10 bg-white/5 p-4">
                <p className="text-xs uppercase tracking-[0.22em] text-white/45">Espera</p>
                <p className="mt-3 text-3xl font-semibold">15m</p>
                <div className="mt-4 h-2 overflow-hidden rounded-full bg-white/10">
                  <div className="h-full w-2/5 rounded-full bg-gradient-to-r from-cyan-400 to-fuchsia-400" />
                </div>
                <p className="mt-2 text-xs text-white/45">Se activa al consumir todos los usos.</p>
              </div>
            </div>

            <div className="mt-8">
              <p className="mb-3 text-xs uppercase tracking-[0.22em] text-white/45">Preset rápido</p>
              <div className="space-y-2">
                {presets.map((item) => (
                  <button
                    key={item.name}
                    className={`w-full rounded-2xl px-4 py-3 text-left text-sm transition ${
                      item.active
                        ? "bg-gradient-to-r from-cyan-400 to-fuchsia-500 text-slate-950 shadow-[0_10px_30px_rgba(34,211,238,0.25)]"
                        : "border border-white/10 bg-white/5 text-white/70 hover:bg-white/10"
                    }`}
                  >
                    {item.name}
                  </button>
                ))}
              </div>
            </div>

            <div className="mt-auto rounded-3xl border border-cyan-400/20 bg-cyan-400/10 p-4">
              <p className="text-sm font-medium text-cyan-200">Consejo visual</p>
              <p className="mt-2 text-sm leading-6 text-cyan-100/70">
                Esta versión cambia por completo la jerarquía, el color y la disposición del contenido.
              </p>
            </div>
          </div>
        </aside>

        <main className="p-4 md:p-6 lg:p-8">
          <div className="grid gap-6 xl:grid-cols-[minmax(0,1.15fr)_380px]">
            <section className="space-y-6">
              <div className="rounded-[2rem] border border-white/10 bg-white/5 p-5 shadow-2xl shadow-black/20 backdrop-blur-xl md:p-6">
                <div className="flex flex-col gap-5">
                  <div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between">
                    <div>
                      <p className="text-xs uppercase tracking-[0.22em] text-cyan-300/80">Entrada</p>
                      <h2 className="mt-2 text-2xl font-semibold">Texto original</h2>
                      <p className="mt-2 text-sm text-white/55">Zona principal para escribir o pegar el contenido a mejorar.</p>
                    </div>

                    <div className="flex items-center gap-2 rounded-full border border-white/10 bg-black/20 px-3 py-2 text-xs text-white/60">
                      <span className="h-2 w-2 rounded-full bg-cyan-400 animate-pulse" />
                      Editor preparado
                    </div>
                  </div>

                  <div className="rounded-[1.75rem] border border-white/10 bg-[#0f172d] p-3">
                    <div className="rounded-[1.25rem] border border-white/10 bg-[#0b1327] p-4">
                      <div className="mb-3 flex items-center justify-between text-xs text-white/40">
                        <span>Borrador actual</span>
                        <span>287 / 500</span>
                      </div>

                      <div className="min-h-[220px] rounded-[1.25rem] border border-dashed border-white/10 bg-white/[0.03] p-4 text-sm leading-7 text-white/75">
                        Hola, quería pedir información sobre el proyecto y saber si podéis ayudarme a dejar este texto mejor redactado para enviarlo por correo a un cliente potencial.
                      </div>

                      <div className="mt-4 flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
                        <div className="flex-1">
                          <div className="mb-2 flex items-center justify-between text-xs text-white/40">
                            <span>Límite de caracteres</span>
                            <span>57%</span>
                          </div>
                          <div className="h-2 overflow-hidden rounded-full bg-white/10">
                            <div className="h-full w-[57%] rounded-full bg-gradient-to-r from-cyan-400 via-sky-400 to-fuchsia-500" />
                          </div>
                        </div>

                        <div className="flex flex-wrap gap-3">
                          <button className="rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-white/70 hover:bg-white/10">
                            Vaciar
                          </button>
                          <button className="rounded-2xl bg-gradient-to-r from-cyan-400 to-fuchsia-500 px-5 py-3 text-sm font-medium text-slate-950 shadow-[0_14px_35px_rgba(56,189,248,0.22)]">
                            Redactar vista previa
                          </button>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>

              <div className="grid gap-6 md:grid-cols-2">
                <div className="rounded-[2rem] border border-white/10 bg-white/5 p-5 backdrop-blur-xl">
                  <div className="flex items-center justify-between gap-3">
                    <div>
                      <p className="text-xs uppercase tracking-[0.22em] text-fuchsia-300/80">Salida</p>
                      <h3 className="mt-2 text-xl font-semibold">Texto generado</h3>
                    </div>
                    <button className="rounded-2xl border border-white/10 bg-white/5 px-4 py-2.5 text-sm text-white/70 hover:bg-white/10">
                      Copiar
                    </button>
                  </div>

                  <div className="mt-4 rounded-[1.5rem] border border-fuchsia-400/20 bg-fuchsia-400/5 p-4">
                    <div className="mb-3 inline-flex items-center gap-2 rounded-full bg-amber-400/10 px-3 py-1 text-xs text-amber-200">
                      <span className="h-2 w-2 rounded-full bg-amber-300 animate-pulse" />
                      Simulación de generación
                    </div>
                    <p className="text-sm leading-7 text-white/78">
                      Hola, me gustaría solicitar información sobre el proyecto y conocer de qué manera podríais ayudarme a mejorar este mensaje para presentarlo correctamente por correo a un posible cliente. Busco una redacción más clara, profesional y convincente.
                    </p>
                  </div>
                </div>

                <div className="rounded-[2rem] border border-white/10 bg-white/5 p-5 backdrop-blur-xl">
                  <p className="text-xs uppercase tracking-[0.22em] text-rose-300/80">Límite de uso</p>
                  <h3 className="mt-2 text-xl font-semibold">Bloqueo temporal</h3>
                  <div className="mt-4 rounded-[1.5rem] border border-rose-400/20 bg-rose-400/8 p-4">
                    <p className="text-sm leading-7 text-white/75">
                      Has consumido los 5 intentos disponibles. El acceso se reactiva al terminar el periodo de espera.
                    </p>
                    <div className="mt-4 h-2 overflow-hidden rounded-full bg-white/10">
                      <div className="h-full w-4/5 rounded-full bg-gradient-to-r from-rose-400 to-orange-400" />
                    </div>
                    <div className="mt-3 flex items-center justify-between text-xs text-white/45">
                      <span>Reinicio estimado</span>
                      <span>15:00 min</span>
                    </div>
                  </div>
                </div>
              </div>
            </section>

            <section className="space-y-6">
              <div className="rounded-[2rem] border border-white/10 bg-white/5 p-5 backdrop-blur-xl">
                <p className="text-xs uppercase tracking-[0.22em] text-cyan-300/80">Panel creativo</p>
                <h2 className="mt-2 text-2xl font-semibold">Cómo quieres que suene</h2>
                <p className="mt-2 text-sm leading-6 text-white/55">
                  Selector visual de intención, tono y fuerza de reescritura.
                </p>

                <div className="mt-5 grid grid-cols-2 gap-3">
                  {[
                    "Más formal",
                    "Más casual",
                    "Más divertido",
                    "Más profesional",
                    "Más breve",
                    "Más persuasivo",
                  ].map((item, index) => (
                    <button
                      key={item}
                      className={`rounded-2xl px-4 py-3 text-left text-sm transition ${
                        index === 0
                          ? "bg-white text-slate-950"
                          : "border border-white/10 bg-white/5 text-white/70 hover:bg-white/10"
                      }`}
                    >
                      {item}
                    </button>
                  ))}
                </div>
              </div>

              <div className="rounded-[2rem] border border-white/10 bg-white/5 p-5 backdrop-blur-xl">
                <div className="flex items-center justify-between">
                  <div>
                    <p className="text-xs uppercase tracking-[0.22em] text-cyan-300/80">Nivel</p>
                    <h3 className="mt-2 text-lg font-semibold">Intensidad del ajuste</h3>
                  </div>
                  <span className="rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-white/55">
                    Alto
                  </span>
                </div>

                <div className="mt-5 rounded-full bg-white/10 p-1">
                  <div className="relative h-3 rounded-full bg-transparent">
                    <div className="absolute inset-y-0 left-0 w-[72%] rounded-full bg-gradient-to-r from-cyan-400 to-fuchsia-500" />
                    <div className="absolute left-[72%] top-1/2 h-6 w-6 -translate-x-1/2 -translate-y-1/2 rounded-full border-4 border-[#0a0f1f] bg-white shadow-[0_0_0_6px_rgba(255,255,255,0.08)]" />
                  </div>
                </div>
              </div>

              <div className="rounded-[2rem] border border-white/10 bg-white/5 p-5 backdrop-blur-xl">
                <p className="text-xs uppercase tracking-[0.22em] text-cyan-300/80">Instrucción extra</p>
                <div className="mt-4 rounded-[1.5rem] border border-dashed border-white/10 bg-black/20 p-4 text-sm leading-7 text-white/38">
                  Ej.: Mantén el mensaje claro, elegante y convincente, sin que parezca demasiado robótico.
                </div>
              </div>

              <div className="rounded-[2rem] border border-white/10 bg-gradient-to-br from-cyan-400/10 to-fuchsia-500/10 p-5 backdrop-blur-xl">
                <p className="text-xs uppercase tracking-[0.22em] text-white/45">Estado visual</p>
                <div className="mt-4 space-y-3">
                  {[
                    ["Motor de redacción", "Activo"],
                    ["Filtro aplicado", "Formal"],
                    ["Tiempo estimado", "1.2s"],
                  ].map(([label, value]) => (
                    <div key={label} className="flex items-center justify-between rounded-2xl border border-white/10 bg-black/20 px-4 py-3 text-sm">
                      <span className="text-white/55">{label}</span>
                      <span className="font-medium text-white">{value}</span>
                    </div>
                  ))}
                </div>
              </div>
            </section>
          </div>
        </main>
      </div>
    </div>
  );
}
